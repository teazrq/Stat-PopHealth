---
title: "Week 2: R Intro and Basic Statistics"
knit: (function(input_file, encoding) {
  out_dir <- 'docs';
  rmarkdown::render(input_file,
 encoding=encoding,
 output_file=file.path(dirname(input_file), out_dir, 'week2.html'))})
date: "Last Updated: `r format(Sys.time(), '%B %e, %Y')`"
fontsize: 12pt
output: 
  html_document:
    includes:
      in_header: "favicon.html"
---

## Weekly Objectives

  * Install R and R Studio
  * Install R packages
  * Load and save data using Excel
  * Using and writing R functions
  * Calculate summary statistics
  * Generate random numbers and set random seed
  
## Lecture Note

## R Examples
